# ---------------------------------------------------------------------------
# PRODUCTION Docker build (optional — for docker compose build args)
#   cp .env.docker.example .env.docker
#
# Browser calls API from the host machine, so use host-published backend port:
#   http://localhost:8000/api
#
# If you later use one domain + reverse proxy, rebuild with e.g. /api
# ---------------------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:8000/api
